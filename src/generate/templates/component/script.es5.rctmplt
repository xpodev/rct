import "./{{name}}.css";
import { render } from "./{{name}}.render";
{@@typescript:
export interface {{name}}Props {
    
}
@}
function {{name}} (props{@@typescript:: {{name}}Props@}) {
    return render(props);
}

export default {{name}};
