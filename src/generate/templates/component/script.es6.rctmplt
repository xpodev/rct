import { Component } from 'react';
import "./{{name}}.css";
import { render } from "./{{name}}.template";
{@@typescript:
interface {{name}}Props {
    
}
    
interface {{name}}State {
    
}
@}
class {{name}} extends Component{@@typescript:<{{name}}Props, {{name}}State>@} {
    render() {
        return render.call(this);
    }
}

export default {{name}};
